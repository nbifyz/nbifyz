hooks:
  before_constitution:
    - id: pre_constitution
      enabled: true
      optional: false
      extension: "your-extension"
      command: "pre_constitution"
      description: "Run before /agents.constitution"

  after_constitution:
    - id: post_constitution
      enabled: true
      optional: true
      extension: "your-extension"
      command: "post_constitution"
      description: "Run after /agents.constitution"
      prompt: "Run post-constitution hook?"

  before_specify:
    - id: pre_specify
      enabled: true
      optional: false
      extension: "your-extension"
      command: "pre_specify"
      description: "Run before /agents.specify"

  after_specify:
    - id: post_specify
      enabled: true
      optional: true
      extension: "your-extension"
      command: "post_specify"
      description: "Run after /agents.specify"
      prompt: "Run post-specify hook?"

  before_plan:
    - id: pre_plan
      enabled: true
      optional: false
      extension: "your-extension"
      command: "pre_plan"
      description: "Run before /agents.plan"

  after_plan:
    - id: post_plan
      enabled: true
      optional: true
      extension: "your-extension"
      command: "post_plan"
      description: "Run after /agents.plan"
      prompt: "Run post-plan hook?"

  # before_tasks и after_tasks у вас уже есть — их можно оставить

  # before_implement и after_implement уже есть

  # Опциональные команды:
  before_clarify:
    - id: pre_clarify
      enabled: true
      optional: true
      extension: "your-extension"
      command: "pre_clarify"
      description: "Run before /agents.clarify"

  after_clarify:
    - id: post_clarify
      enabled: true
      optional: true
      extension: "your-extension"
      command: "post_clarify"
      description: "Run after /agents.clarify"
      prompt: "Run post-clarify hook?"

  before_analyze:
    - id: pre_analyze
      enabled: true
      optional: true
      extension: "your-extension"
      command: "pre_analyze"
      description: "Run before /agents.analyze"

  after_analyze:
    - id: post_analyze
      enabled: true
      optional: true
      extension: "your-extension"
      command: "post_analyze"
      description: "Run after /agents.analyze"
      prompt: "Run post-analyze hook?"

  before_checklist:
    - id: pre_checklist
      enabled: true
      optional: true
      extension: "your-extension"
      command: "pre_checklist"
      description: "Run before /agents.checklist"

  after_checklist:
    - id: post_checklist
      enabled: true
      optional: true
      extension: "your-extension"
      command: "post_checklist"
      description: "Run after /agents.checklist"
      prompt: "Run post-checklist hook?"